import { NoHtmlPipePipe } from './no-html-pipe.pipe';

describe('NoHtmlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoHtmlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
